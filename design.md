
### Books
```js
getBooks(searchParams); // Get all books;
getBookItem(bookId); // Get book by id;
getBookChapters(bookId, searchParams); // Get book chapters by book id
```

### Movies
```js
getMovies(searchParams); // Get all movies
getMovieItem(movieId); // Get movie by id
getMovieQuotes(movieId, searchParams); // Get movie quotes by movie id
```
### Characters

```js
getCharacters(searchParams); // Get all characters
getCharacterItem(characterId); // Get character by id
getCharacterQuotes(characterId, searchParams); // Get character quotes by character id
```
### Chapters
```js
getChapters(searchParams); // Get all chapters
getChapterItem(chapterId); // Get chapter by id
```
### Quotes
```js
getQuotes(searchParams); // Get all quotes
getQuoteItem(quoteId); // Get quote by id
```

